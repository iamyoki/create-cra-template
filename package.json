{
  "name": "make-cra-template",
  "private": true,
  "workspaces": [
    "packages/*",
    "packages/templates/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0",
    "typescript": "^4.3.2"
  },
  "scripts": {
    "start": "lerna run --parallel start",
    "build": "lerna run --parallel start"
  },
  "dependencies": {}
}
